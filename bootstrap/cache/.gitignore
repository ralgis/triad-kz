*
!.gitignore
# packages.php is normally generated by `php artisan package:discover`,
# but on Plesk shared hosting that runs through proc_open (disabled).
# We pre-generate it locally + commit so Laravel doesn't need to discover
# at boot time. Regenerate with `php artisan package:discover` after
# adding/removing a Composer package.
!packages.php
